{
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "dependencies": {
    "ember-template-lint": "^1.1.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^5.15.0",
    "jest": "^24.1.0"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "node_modules",
      "tests/helpers"
    ],
    "testMatch": [
      "**/tests/**/*.js"
    ]
  }
}
